---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: activity-service
  namespace: fitness
spec:
  replicas: 1
  selector:
    matchLabels:
      app: activity-service
  template:
    metadata:
      labels:
        app: activity-service
    spec:
      containers:
        - name: activity-service
          image: jayantmule02/activity-service:latest
          ports:
            - containerPort: 8082

          env:
            - name: SERVER_PORT
              value: "8082"

            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-server:8888"

          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 40
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 5

---
apiVersion: v1
kind: Service
metadata:
  name: activity-service
  namespace: fitness
spec:
  selector:
    app: activity-service
  ports:
    - port: 8082
      targetPort: 8082
